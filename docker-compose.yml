services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./models:/app/models        # model persists between container restarts
      - ./mlruns:/app/mlruns        # MLflow runs persist
      - ./templates:/app/templates  # live reload — no rebuild needed for UI changes
    restart: unless-stopped
