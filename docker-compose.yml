services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./models:/app/models        # model persists between container restarts
      - ./mlruns:/app/mlruns        # MLflow runs persist
    restart: unless-stopped
